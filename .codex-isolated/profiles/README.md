# Shared Profile Policy

This directory contains shared, curated profile policy only. Registry changes require
explicit approval before commit, and project manifests pin the exact registry byte hash.

Do not place project manifests, runtime state, authentication data, caches, binaries,
sessions, or live measurements here.
