# Goal

Topic: `smoke-governance-merge-release`

Verify governance merge-release preflight accepts complete release policy in dry-run mode.

Success criterion: approved run contract reaches `result`.
